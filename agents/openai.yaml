interface:
  display_name: "Shared Browser Terminal"
  short_description: "Share your Mac terminal in a password-protected browser"
  default_prompt: "Use $shared-browser-terminal to start or reconnect my shared browser terminal with its saved settings."
policy:
  allow_implicit_invocation: true
